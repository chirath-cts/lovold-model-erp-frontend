# Styling Guidelines

1. Use MUI component props + theme tokens first.
2. Use `sx` for view-level layout and spacing.
3. Use SCSS modules only for custom visual needs that are awkward in `sx`.
4. Keep SCSS token values aligned with `src/theme/theme.ts` and `src/theme/colors.ts`.
